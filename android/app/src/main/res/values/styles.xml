<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off.
         Extends AndroidX's core-splashscreen compat theme (works down to
         the app's minSdk, not just API 31+) so the pre-Flutter-engine
         splash shows our brand mark instead of the default app icon —
         see MainActivity.kt's installSplashScreen() call. -->
    <style name="LaunchTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_pill</item>
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
        <!-- Once the splash-screen API's own icon/background are gone,
             this keeps the window on-brand until Flutter's first frame
             (Dart's own SplashPage) takes over, instead of flashing white. -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
